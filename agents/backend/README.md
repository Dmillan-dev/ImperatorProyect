# Backend Agent

Responsabilidades:

- Implementación del Operational Intelligence Layer (Java, Spring Boot).
- Diseño y operación de pipelines (Kafka, ingestion, Decision Pipeline).
- Diseñar contratos Protobuf y servicios gRPC.
- Persistencia estratégica (PostgreSQL, Decision Ledger).

Entregables iniciales:

- `services/backend/` scaffold con README y ejemplos de contract generation.
- Conjunto inicial de `.proto` y guía de generación (Java).
