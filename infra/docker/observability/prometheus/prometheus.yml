global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: imperator-backend
    metrics_path: /actuator/prometheus
    scheme: http
    static_configs:
      - targets:
          - backend:9090
