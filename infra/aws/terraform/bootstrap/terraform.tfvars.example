aws_region         = "eu-west-1"
environment        = "pilot"
github_owner       = "Dmillan-dev"
github_repository  = "ImperatorProyect"
state_bucket_name  = "replace-with-globally-unique-imperator-state-bucket"

# Set this only when the account already has the GitHub Actions OIDC provider.
# existing_github_oidc_provider_arn = "arn:aws:iam::123456789012:oidc-provider/token.actions.githubusercontent.com"
