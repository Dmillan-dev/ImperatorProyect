# MVP Query Application Tests

This folder verifies the read-only Application use cases frozen by D086,
including projection mapping, causal pagination, and ROI provenance. It must not
contain HTTP, Spring, JDBC, PostgreSQL, or write-behavior tests.
